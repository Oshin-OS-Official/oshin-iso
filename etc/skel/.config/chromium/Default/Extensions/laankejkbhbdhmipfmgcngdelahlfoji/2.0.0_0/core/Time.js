define(()=>{return{SECOND:1e3,MINUTE:6e4,HOUR:36e5,DAY:864e5,getToday(){var now=new Date;return now.setMilliseconds(0),now.setSeconds(0),now.setMinutes(0),now.setHours(0),now.getTime()},getStartOfDay(date){date=new Date(date);return date.setMilliseconds(0),date.setSeconds(0),date.setMinutes(0),date.setHours(0),date.getTime()},formatDurationInSeconds(durationInSec){var hours,seconds;return 0===durationInSec?"00:00:00":(seconds=durationInSec-(3600*(hours=Math.floor(durationInSec/3600))+60*(durationInSec=Math.floor((durationInSec-3600*hours)/60))),(hours=API.Date.toTwoDigits(hours))+`:${durationInSec=API.Date.toTwoDigits(durationInSec)}:`+(seconds=API.Date.toTwoDigits(seconds)))},formatDurationInMilliseconds(durationInMilliseconds){return this.formatDurationInSeconds(Math.floor(durationInMilliseconds/1e3))},formatMillisecondsToTime(milliseconds){var milliseconds=milliseconds/6e4,blockHours=milliseconds<60?0:Math.floor(milliseconds/60);let blockMinutes=0;return{hours:blockHours,minutes:blockMinutes=0<blockHours?milliseconds-60*blockHours:milliseconds}},formatTimeToMilliseconds(hours,minutes){return 36e5*hours+6e4*minutes}}});